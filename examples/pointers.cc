#include <iostream>
using namespace std;

int main() {
  double *p;  // variable p will store a memory location in which a
	      // double resides;
  
  p3 = 60;
  p1 = &p3;  // amperasnd will give q memory location where the int
	     // that p3 stores is.
  cout << p1 << " " << &p3 << endl << *p1 << " " << p3 << endl;  
  p3 = 100;
  
}

  
